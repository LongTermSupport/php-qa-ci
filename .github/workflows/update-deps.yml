name: Update Dependencies

on:
  schedule:
    # Weekly on Sunday at 03:00 UTC
    - cron: '0 3 * * 0'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-and-verify:
    name: Update Dependencies & Verify QA
    runs-on: ubuntu-latest

    steps:
      # No explicit `ref:` — actions/checkout defaults to the repository's own
      # default branch for schedule/workflow_dispatch events. This keeps the
      # workflow correct when a consumer copies it verbatim: it operates on the
      # consumer's default branch, not php-qa-ci's.
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          tools: composer:v2, phive
          coverage: xdebug
          extensions: json, tokenizer, mbstring

      - name: Get composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-update-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-update-
            ${{ runner.os }}-composer-

      # Step 1: Update composer dependencies (post-update-cmd runs the PHAR update
      # too, so the token is needed here as well)
      - name: Update composer dependencies
        env:
          GITHUB_AUTH_TOKEN: ${{ github.token }}
        run: composer update --no-interaction --no-progress --prefer-dist

      # Step 2: Update PHARs. tool-install.bash update is the one place that
      # knows the trusted keys and the unsigned entries; it re-resolves every
      # phive.xml constraint non-interactively. (composer update above already
      # ran it as post-update-cmd; this step makes a failure visible.)
      - name: Update PHARs via phive
        env:
          GITHUB_AUTH_TOKEN: ${{ github.token }}
        run: bash scripts/tool-install.bash update

      # Step 3: Update the self-built PHARs (rector, phpcpd, composer-dependency-analyser)
      # Bump each build/<tool>/ manifest (updates its composer.lock), then rebuild
      # vendor-phar/<tool>.phar. build-phar.bash auto-downloads a pinned Box and
      # sets phar.readonly=0 itself.
      - name: Update self-built PHARs
        run: |
          for manifest in build/*/composer.json; do
            composer update --working-dir="$(dirname "$manifest")" --no-interaction --no-dev
          done
          bash scripts/build-phar.bash --all --force

      # Step 4: Check for changes
      - name: Detect changes
        id: changes
        run: |
          if git diff --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "No dependency changes detected"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Changes detected:"
            git diff --stat
          fi

      # Step 5: Run full QA pipeline (only if changes detected)
      - name: Run QA Pipeline
        if: steps.changes.outputs.changed == 'true'
        env:
          CI: true
          skipUncommittedChangesCheck: 1
          phpUnitQuickTests: 0
          phpUnitCoverage: 0
        run: bash ci.bash

      # Step 6: Generate PR summary
      - name: Generate update summary
        if: steps.changes.outputs.changed == 'true'
        id: summary
        run: |
          echo "body<<EOF" >> "$GITHUB_OUTPUT"
          # Summaries only: a full lock diff can exceed the GITHUB_OUTPUT size
          # limit, and a truncated diff would be misleading. The PR's own diff
          # tab carries the detail.
          echo "## Composer Changes" >> "$GITHUB_OUTPUT"
          echo "" >> "$GITHUB_OUTPUT"
          echo '```' >> "$GITHUB_OUTPUT"
          git diff --stat composer.json composer.lock >> "$GITHUB_OUTPUT"
          echo '```' >> "$GITHUB_OUTPUT"
          echo "" >> "$GITHUB_OUTPUT"
          echo "Package versions after the update:" >> "$GITHUB_OUTPUT"
          echo "" >> "$GITHUB_OUTPUT"
          echo '```' >> "$GITHUB_OUTPUT"
          composer show --direct --no-interaction >> "$GITHUB_OUTPUT"
          echo '```' >> "$GITHUB_OUTPUT"
          echo "" >> "$GITHUB_OUTPUT"

          # Check for PHAR changes
          if ! git diff --quiet phive.xml vendor-phar/; then
            echo "## PHAR Changes" >> "$GITHUB_OUTPUT"
            echo "" >> "$GITHUB_OUTPUT"
            for phar in vendor-phar/*.phar; do
              name=$(basename "$phar" .phar)
              # Only the first line of --version output is the version banner.
              version=$(php "$phar" --version 2>/dev/null | awk 'NR==1' || true)
              echo "- **$name**: ${version:-version unknown}" >> "$GITHUB_OUTPUT"
            done
            echo "" >> "$GITHUB_OUTPUT"
          fi

          # Check for self-built PHAR changes (keyed off each build manifest lock —
          # under reproducible builds the phar binary follows the lock).
          if ! git diff --quiet build/*/composer.lock; then
            echo "## Self-built PHAR Changes" >> "$GITHUB_OUTPUT"
            echo "" >> "$GITHUB_OUTPUT"
            for manifest in build/*/composer.json; do
              name=$(basename "$(dirname "$manifest")")
              version=$(php "vendor-phar/$name.phar" --version 2>/dev/null | awk 'NR==1' || true)
              echo "- **$name**: ${version:-version unknown}" >> "$GITHUB_OUTPUT"
            done
            echo "" >> "$GITHUB_OUTPUT"
          fi

          echo "" >> "$GITHUB_OUTPUT"
          echo "---" >> "$GITHUB_OUTPUT"
          echo "QA pipeline passed with these changes." >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      # Step 7: Create PR (only if QA passed — this step is skipped if ci.bash failed)
      - name: Create Pull Request
        if: steps.changes.outputs.changed == 'true'
        id: create-pr
        uses: peter-evans/create-pull-request@v8
        with:
          branch: chore/update-deps
          delete-branch: true
          title: 'chore(deps): update dependencies'
          body: |
            Automated weekly dependency update.

            ${{ steps.summary.outputs.body }}

            This PR was created automatically by the [update-deps](${{ github.server_url }}/${{ github.repository }}/actions/workflows/update-deps.yml) workflow.
          labels: dependencies,automated
          commit-message: 'chore(deps): update all dependencies'

      - name: Enable auto-merge
        if: steps.create-pr.outputs.pull-request-number
        # A merge commit, never a squash or rebase: both rewrite the PR's commits,
        # so `git branch -d` can no longer see the branch as merged.
        run: gh pr merge --auto --merge "${{ steps.create-pr.outputs.pull-request-number }}"
        env:
          GH_TOKEN: ${{ github.token }}
