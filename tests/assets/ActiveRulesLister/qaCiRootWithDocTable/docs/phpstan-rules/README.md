# PHPStan rules

Some intro text that must not be mistaken for the pipeline lanes table.

## Pipeline lanes

| Identifier          | Name         | Where                              |
| ------------------- | ------------ | ---------------------------------- |
| `phpqaci.phpunit`   | PHPUnit      | phpunit lane                       |
| `phpqaci.infection` | Infection    | infection lane                     |
| -                   | Unregistered | some other row that must not match |

## Some other section

This section must not be scanned for lane rows — `phpqaci.shouldNotBeSeen` |
should never be picked up | infection
