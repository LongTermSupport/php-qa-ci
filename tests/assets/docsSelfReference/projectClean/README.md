# Clean Project

This project runs a pipeline. Previously the pipeline was configured in Bash; it is
now configured in PHP, and `qaConfig/qa.php` is the entry point.

`useArkitect=0` was the old spelling and still works; prefer `withArkitect(false)`.
