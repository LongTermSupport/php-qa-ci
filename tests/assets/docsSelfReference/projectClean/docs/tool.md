# Tool

The tool reads the registry. The registry is the single source of truth; consult it
if in doubt.

An earlier release of the tool shipped three lanes this way before they moved to
PHARs, which is why the rule exists.
