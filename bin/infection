#!/usr/bin/env bash
source "$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)/lib-redirect-stub.inc.bash"
phpQaCiRedirectStub "Infection" \
    "  __QACMD__ -t infection                # run mutation testing"
