#!/usr/bin/env bash
source "$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)/lib-redirect-stub.inc.bash"
phpQaCiRedirectStub "PHP CS Fixer" \
    "  __QACMD__ -t fixer                    # fix full codebase" \
    "  __QACMD__ -t fixer -p src/Controller  # fix specific path"
