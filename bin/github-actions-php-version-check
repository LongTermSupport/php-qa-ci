#!/usr/bin/env php
<?php declare(strict_types=1);

require __DIR__.'/bootstrap.php';

if (!isset($argv[1]) || '' === $argv[1]) {
    fwrite(\STDERR, "Usage: github-actions-php-version-check <project-root>\n");
    exit(1);
}

die(\LTS\PHPQA\GithubActions\WorkflowPhpVersionCheck::main($argv[1]));
