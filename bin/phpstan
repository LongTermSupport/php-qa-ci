#!/usr/bin/env bash
source "$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)/lib-redirect-stub.inc.bash"
phpQaCiRedirectStub "PHPStan" \
    "  __QACMD__ -t phpstan                  # analyse full codebase" \
    "  __QACMD__ -t phpstan -p src/Service   # analyse specific path" \
    "  __QACMD__ -t phpstan -p src/Entity    # analyse specific folder"
