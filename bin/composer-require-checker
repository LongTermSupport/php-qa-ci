#!/usr/bin/env bash
source "$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)/lib-redirect-stub.inc.bash"
phpQaCiRedirectStub "Composer Require Checker" \
    "  __QACMD__ -t cr                       # check all dependencies"
