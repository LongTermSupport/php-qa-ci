# Plan 00004 — Journal 26-09-09

> **Append-only activity log** for plan 00004. One file per day
> (`00004-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 09:11 · action · — — plan scaffolded

Plan 00004 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 09:20 · finding · — — DBF 3.1 record: inconsistently documented promoted properties

**Class**: a constructor whose promoted properties are documented some-but-not-all.
**Hazard**: a reader cannot tell whether an undocumented member is self-explanatory or
forgotten, and the documented ones invite comments that restate the name; the mixed state
is what makes both the sloppy comment and the missing one invisible in review.

**Instance**: `QaConfigDto::$useComposerAudit` given a comment while its neighbours had none
(reverted by hand before this record; the class still has 7 of 23 documented).

**Two independent search techniques, chosen before the rule existed**:

1. AST walk (`untracked/scratch/dbf-search.php`, nikic parser, code independent of the
   rule): every `__construct` with two or more promoted params, counting those with a doc
   comment. Found **13** constructors in `src/` and `tests/`: JustificationFindingDto,
   RunRequestDto, InfectionOptionsDto, ProjectPathsDto, QaConfigDto, BranchPolicyConfigDto,
   BranchVerdictDto, LockInfoDto, ProcessResultDto, ProcessSpecDto, ToolDefinitionDto,
   ToolResultDto, ToolContext.
2. Text search: `grep -B1` for a `/** ... */` line directly above a promoted parameter,
   counted per file against the promoted-parameter count per constructor. Found the same
   13 plus files where every promoted param is documented (PhpUnitOptionsDto,
   Psr4Validator, FakeProcessRunner, ToolRegistry), which are not instances. Nothing the
   AST walk missed; saturated.

**Bounds**: constructors with fewer than two promoted properties cannot be inconsistent and
are excluded. Next wider rule: non-promoted constructor parameters documented
some-but-not-all via `@param`. Not built: those share one docblock, PHPStan's own
`missingType.*` checks report a partial `@param` list where a type is involved, and the
search found no mixed case.

**Runner check**: none. Coding-standard class with no runtime behaviour to assert; the
rule is the whole defence.

## 09:25 · finding · — — DBF 3.1 record: repeated string literal within one class

**Class**: the same string literal written three or more times inside one class instead of
being defined once as a constant.
**Hazard**: the value has no single definition, so an edit to one occurrence leaves stale
siblings, and the reader cannot tell what the value means or whether two occurrences are
meant to be equal.

**Instance**: `'8.5.10'` written 20 times across `ComposerChecksToolTest`.

**Two independent search techniques, chosen before the rule existed**:

1. AST walk (same scratch script): per class, every `String_` that is not an attribute
   argument, an array key or an array-dimension index, of length three or more and not
   whitespace-only; reported at three or more occurrences. Found **197** (16 in `src/`,
   181 in `tests/`, `tests/assets` fixtures excluded).
2. Text search: per file, `grep -o "'[^']{3,}'" | sort | uniq -c` at three or more. Found
   200 lines over 89 files; the difference is the grep counting array keys and attribute
   arguments and seeing single-quoted strings only. Every AST instance appeared in the grep
   set; the grep found no excluded value that carries the hazard.

**Bounds** (each exclusion with its sentence):
- Array keys and array-dimension indexes: the repeated key is how an array shape is
  spelled, and PHPStan's array-shape typing reports a misspelt key where the shape is
  typed; a constant per key would obscure the shape.
- Attribute arguments: declarative metadata read by tooling, not values the class
  computes with.
- Fewer than three characters, or whitespace-only: too short to carry meaning that could
  drift.
- Fewer than three occurrences: two is as often coincidence as identity.
- A nested class-like is counted by its own InClassNode.

Next wider rule: the same literal repeated across classes in one namespace. Not built: a
cross-class constant is a design decision (which class owns it) and PHPStan analyses one
file at a time; the search found no cross-class repetition that a single-class rule leaves
unaddressed inside any one class.

**Runner check**: none; coding-standard class, the rule is the defence.

**Sweep decision**: all first-party source (`src/`, `tests/` excluding `tests/assets`
fixtures, `qaConfig/`); `vendor/` and `vendor-phar/` excluded as third-party.

## 09:50 · decision · — — the docs class widened, then bounded to declarations

Owner review of the promoted-property rule: the class is consistent documentation in
general, not promoted properties. Re-measured with the AST technique over three member
kinds, "documented" meaning a docblock with free text (tag-only docblocks are type
metadata):

| kind | mixed classes |
| --- | --- |
| constants (class constants + enum cases) | 10 |
| properties (declared + promoted) | 15 |
| methods (excluding `__construct`) | 58 |

Methods are excluded from the class, with this narrowing sentence: a method's docblock
explains a body the reader can read, whereas a declaration has only its name and type, so
a comment there is the only place meaning beyond the name can live, and that is where mixed
coverage misleads. Forcing all-or-none over methods would either strip the one genuinely
needed explanation in a class of eleven methods or manufacture ten comments that restate
names; the Owner's direction is that the fix must be able to remove bloat, never to create
it. Class as built: `RequireConsistentMemberDocsRule`, `phpqaci.consistentMemberDocs`,
per-kind over constants and properties. Instance count from the search: 25.

## 10:15 · action · — — DBF 3.4 sweep and fix: 221 instances, all fixed

Sweep through the entry point (`QA_READONLY=1 CI=true bin/qa -t stan --json`) recorded
**221 instances in 110 files** before any fixing: 196 `repeatedStringLiteral`, 25
`consistentMemberDocs`. Corroborated by the clause 3.1 searches, which found nothing the
rules missed.

Three subagents were dispatched over disjoint file batches and all three died mid-flight on
a usage limit. They left a systematic defect: each had added a constant and then run a text
replace-all for the literal, which rewrote the constant's own declaration into
`private const string X = self::X;` — eleven self-referential constants across eight files,
all of which pass `php -l` and fatal only at use. Repaired by hand from the literals the
sweep record named. **Lesson for the next dispatch: a literal-extraction edit must be
position-based, never a text replace-all.**

The rest was then done here with an AST-positioned rewriter
(`untracked/scratch/extract-literals.php`): it takes the byte positions of the occurrences
from the parser, inserts the declaration, and rewrites only those positions, so a
declaration can never become a self-reference. 71 files, 145 constants, applied by pattern
with the names generated from the literal and 43 of them overridden by hand
(`untracked/scratch/literal-names.php`) where a derived name would have been poor
(`VALUE_8_5_10` became `PHP_VERSION`, banner rules became `RULE`, and so on). Sampled the
output rather than examining all 145: the whole suite is the sample, plus the by-hand review
of the 40 that survived.

**Narrowing recorded (clause 3.3).** The 18 survivors in `ToolRegistryCharacterisationTest`
were all inside `private const array` golden tables. A class-constant declaration is already
the single named definition the rule asks for, so its own value can never carry the hazard;
the rule now skips anything inside a `ClassConst`, and the docs page says so with the golden
table as the example. This is a narrowing, not a suppression: the hazard cannot arise there.

**Docs instances.** All 22 fixed by moving the notes that carried real meaning into the
class docblock and deleting the per-member comments, per the Owner's direction that the fix
must be able to remove bloat rather than manufacture it. No member gained a comment that
restates its name.

Final sweep: **0 instances, 0 other errors**; suite green at 795 tests.

## 10:40 · finding · — — DBF review of composer.json `suggest`

Owner asked whether every `suggest` entry is still valid and 8.5-compatible. Checked each
against the Packagist p2 API today:

| entry | latest | released | php | verdict |
| --- | --- | --- | --- | --- |
| nunomaduro/larastan | v3.11.0 | 2026-09-01 | `^8.2` | ABANDONED, renamed `larastan/larastan` |
| phploc/phploc | 7.0.2 | 2020-12-07 | `>=7.3` | ABANDONED, dead six years |
| phpstan/phpstan-doctrine | 2.0.28 | 2026-07-13 | `^7.4 \|\| ^8.0` | keep |
| phpstan/phpstan-mockery | 2.0.0 | 2024-10-14 | `^7.4 \|\| ^8.0` | keep; stable, no release needed, admits 8.5 |
| phpstan/phpstan-phpunit | 2.0.18 | 2026-07-04 | `^7.4 \|\| ^8.0` | REDUNDANT: already in `require` |
| phpstan/phpstan-symfony | 2.0.20 | 2026-06-16 | `^7.4 \|\| ^8.0` | keep |
| sllh/composer-versions-check | v2.0.5 | 2021-07-12 | `^7.4 \|\| ^8.0` | dead four years; its blurb ("to be brought back when there is a PHP8 version") was already false of v2.0.5 |

**Class**: a `suggest` entry that misleads — it names a package the consumer already gets, or
one that is abandoned or long dead.
**Hazard**: `suggest` is advice a consumer acts on. Advice to install something already
required is noise that trains the reader to ignore the block; advice to install an abandoned
package spends their time and adds an unmaintained dependency.

**Rule attempted, and split in two:**

- The *redundant* half is decidable from composer.json alone: an entry present in both
  `suggest` and `require`/`require-dev`. Built, as a check inside the existing composerChecks
  lane rather than a new lane, since it is composer.json hygiene and that lane already owns
  that. No new identifier or docs page.
- The *abandoned/dead* half is **not** decidable offline: it needs the Packagist API, and the
  pipeline fetches nothing at run time. Recorded as a toolchain gap with that reason. The
  nearest thing the toolchain has is `composer audit --abandoned=report`, added in Task 1.1,
  which reports abandoned packages that are actually *installed*; a suggest is by definition
  not installed, so it is out of that command's reach. Those instances are fixed
  conventionally.

**Search**: `suggest` has seven entries in one file, all read above; there is no second
manifest in the repository (`find . -name composer.json -not -path '*/vendor/*'` returns the
root and the test fixtures, and no fixture declares a suggest).

## 10:55 · finding · — — the shipped PHAR loads Composer-installed PHPStan extensions

Task 2.3 and Task 3.2 both rest on one unverified assumption: that PHPStan run *from the
shipped PHAR* (`vendor-phar/phpstan.phar`) still picks up an extension installed by
**Composer** into the project's `vendor/`. If it did not, every extension we adopt would have
to become a PHIVE PHAR, which most of them do not publish.

Confirmed empirically rather than from the documentation. `phpstan/phpstan-deprecation-rules`
exists only as a Composer `require`; it is in no neon this repository ships, and the wrapper
neon the phpstan lane generates includes nothing but the resolved `phpstan.neon` and a
parallelism cap. A probe class carrying a `@deprecated` method and a call to it, analysed by
the PHAR against the shipped `configDefaults/generic/phpstan.neon`, reported:

    Call to deprecated method old() of class Probe\Probe [identifier=method.deprecated]

That rule ships in `phpstan-deprecation-rules` and nowhere else, so the PHAR resolved
`vendor/phpstan/extension-installer/src/GeneratedConfig.php` on its own. Route confirmed.

**What it unblocks**: Task 2.3 can wire `tomasvotruba/type-coverage` as a plain Composer
dependency with no PHIVE work, and Task 3.2's stated precondition ("confirm the PHAR-run
PHPStan loads a Composer-installed extension") is met, so that decision turns on the
false-positive surface alone.

**What it does not license**: an extension arriving this way is invisible in the neon, so
nothing in the repository states which rules are active. `bin/rules` remains the only listing;
any extension adopted through this route needs its identifiers documented on the phpstan page,
which is already Task 2.3's requirement and now applies to Task 3.2 as well.

## 11:00 · action · — — Phase 1 closed

Tasks 1.1 (`composer audit` in the composerChecks lane), 1.2 (deprecation-rules promoted to
`require`) and 1.3 (the `suggest` audit and `RedundantSuggestDetector`) are done and committed.
Phase 2 opens with reconnaissance on the four tools' real integration contracts — PHIVE
availability, executable names, config file formats and exit codes — because three of the
five tasks are shaped by facts we had only assumed.

## 11:10 · finding · — — the phploc lane is already dead, which reshapes Task 2.4

Task 2.4 says to add phpcpd "as an informational post-success lane next to phploc". Checking
what "next to phploc" actually means found there is nothing there to sit next to.

- `phploc/phploc` on Packagist: latest release **7.0.2, 2020-12-07**, `abandoned: true`. The
  original `sebastian/phploc` is archived. There is no maintained successor under either name.
- `PhplocTool` resolves `{binDir}/phploc` and returns `skipped('phploc not installed')` when it
  is absent. It is absent on this repository (`bin/` holds no `phploc`), and it is not in
  `phive.xml`, so the pipeline never ships it. The lane can only ever execute in a consumer
  that has deliberately installed an abandoned package.
- The `suggest` audit at 10:40 removed `phploc/phploc` from the manifest for exactly this
  reason, which means the pipeline no longer even advertises the thing its own lane needs.

So the post-success phase currently contains one lane that is unreachable by construction.
Adding phpcpd beside it would leave two informational lanes where one has been inert for five
years and cannot be revived.

**Owner decision required** — this is a lane removal, which the method reserves to the Owner:

1. **Replace**: `PhpcpdTool` takes the post-success slot and `PhplocTool` is deleted, with its
   docs page and registry row. One maintained informational lane, no dead code.
2. **Add alongside**: implement Task 2.4 as written and leave `PhplocTool` in place, on the
   grounds that a consumer may still have phploc installed and the skip costs nothing.
3. **Replace and keep the metric**: phpcpd-next reports duplication, not size. If the size
   statistics are wanted, they need a different maintained source; none was found in the
   ecosystem survey.

Recorded, not acted on. Task 2.4 stays open pending the answer.

## 11:25 · decision · joseph — PhplocTool deleted; phpcpd is not its replacement

Owner ruling on the 11:10 finding: "just drop the size metric stuff its not important", and a
correction to the way the question was framed — phpcpd is **not** a replacement for phploc.
That is right. phploc counts size and complexity; phpcpd detects copy/paste. Calling one the
successor of the other conflated two unrelated signals, and the plan's own wording ("next to
phploc") had carried that confusion in from the ecosystem survey.

So the two are now separate items. `PhplocTool` is deleted outright because the size metric is
not wanted, not because something else covers it. Task 2.4b adds phpcpd on its own merits as a
new signal the pipeline has never had.

Removed: `src/Pipeline/Lane/PhplocTool.php`, `tests/Small/Pipeline/Lane/PhplocToolTest.php`,
`docs/tools/phploc.md`, the `ShippedTools` wiring, the `ToolRegistry` definition with its `l`
and `loc` aliases, the `Pipeline` post-success call and its "Statistics:" banner, the index row
in `docs/phpstan-rules/README.md`, the override row in `docs/upgrading-to-8.5.md`, the sections
in `CLAUDE.md`, `docs/pipeline.md` and `docs/phpqa-tools.md`, and the two tool lists in
`scripts/setup-claude-qa-agent.bash`.

Two test assertions went with it. `PipelineTest` asserted `phploc` had not run in the two cases
where the pipeline aborts early; both tests already assert `ALL TESTS PASSING` is absent, which
is the same invariant — the post-success phase was never reached — so nothing is now unchecked.
`ActiveRulesListerTest` used phploc as its example of a lane with a null phase; `uniterate` is
the remaining one and takes over that role.

The post-success phase now holds only the project's `hookPost.php`.

## 11:40 · finding · — — Phase 2 and 3 reconnaissance

Two agents verified the integration contracts we had been assuming. Full reports in
`subagent-reports/260909-phase2-recon-sonnet.md` and `260909-phase3-recon-sonnet.md`. What
changes the plan:

| Tool | Install route | Executable | Config file | Exit contract |
| --- | --- | --- | --- | --- |
| shipmonk/composer-dependency-analyser 1.8.4 | Composer (no PHIVE PHAR published) | `bin/composer-dependency-analyser` | `composer-dependency-analyser.php` | 0 clean, 1 **findings or error** |
| tomasvotruba/type-coverage 2.3.6 | Composer, `extra.phpstan.includes` | n/a, phpstan extension | `parameters.type_coverage` | n/a |
| phpcpd-next/phpcpd 1.4 | Composer (no PHIVE PHAR) | `bin/phpcpd` | `phpcpd.ini` | 0 clean, 1 **clones or error** |
| vincentlanglet/twig-cs-fixer 4.1.1 | **PHIVE**, alias `twig-cs-fixer` | `bin/twig-cs-fixer` | `.twig-cs-fixer.php` | 0 clean, 1 violations, 2 crash |

Three facts we had wrong and that shape the work:

1. **Only twig-cs-fixer publishes a signed PHAR.** The plan assumed composer-dependency-analyser
   and phpcpd would go through PHIVE. Neither attaches a `.phar` to its releases and neither is
   in `phar.io/data/repositories.xml`, so both become Composer dependencies. Acceptable here
   because both declare **zero** Composer runtime dependencies (extensions only), so neither can
   drag a conflicting package into a consumer's graph. type-coverage does bring
   `webmozart/assert`, which is the one new transitive edge in this batch.

2. **Two of the three exit-code contracts are overloaded.** Both the analyser and phpcpd return
   `1` for "found something" *and* for "could not run". Neither lane can tell a finding from a
   crash by exit code alone, so neither may use `ToolResultDto::fromExitCode`. phpcpd is meant to
   be informational, which the overload makes awkward: swallowing `1` swallows its crashes too.
   The lane will treat 0 and 1 as informational and anything else as a crash, and say so on its
   page. twig-cs-fixer's 0/1/2 maps cleanly onto passed/failed/crashed.

3. **type-coverage's rules mostly emit no identifier.** Only `DeclareCoverageRule` sets one
   (`typeCoverage.declareCoverage`); the return, param, property and constant rules build their
   errors without calling `->identifier()`. That breaks the assumption behind Task 2.3's
   "document the extension's error identifiers" — there are none to document for four of the
   five, and a consumer who needs to suppress one can only match on message text, which our
   `phpstanIgnoreJustification` lane cannot tie to an identifier. Recorded as a constraint on
   that task, not a blocker.

The canonical neon keys are `return_type`, `param_type`, `property_type`, `constant_type` and
`declare` (not `declare_strict_types`); the short `return`/`param`/`property`/`constant` forms in
the README are aliases. `constant_type` and `declare` exist, so the builder method the plan
specified as `withTypeCoverageFloors(int $return, int $param, int $property)` is missing two
dimensions.

## 11:45 · decision · — — Phase 3 recommendations, for the Owner

**Task 3.1, spaze/phpstan-disallowed-calls: adopt alongside, do not retire our rule.**

Retiring `ForbidDangerousFunctionsRule` was the attractive option and the evidence rules it out.
Of the ten functions we ban, eight are covered by two shipped bundles
(`disallowed-execution-calls.neon`, `disallowed-dangerous-calls.neon`), but `unserialize` and
`parse_str` appear in none of the five, so we would have to author and maintain those entries
ourselves. Worse, our rule exempts the two-argument `parse_str($s, $out)` and flags only the
one-argument form; spaze's allow-directives condition on parameter *values*, not on argument
*count*, so that exemption appears to be inexpressible — retiring would either over-report or
need a custom rule class, which is what we already have.

The decisive point is enforcement, not coverage. spaze bakes `allowIn`, `allowExceptIn`,
`allowInInstanceOf` and attribute-based exclusions into the same config layer as the ban. Our
`phpstanIgnoreJustification` lane audits `ignoreErrors` entries and nothing else, so a consumer
could add `allowIn: [tests/*]` to `eval()` and no lane would ask them to justify it. Adopting
spaze as the *only* defence would therefore open a silent narrowing route that the toolchain
cannot see — precisely the failure the justification lane exists to close.

So: keep `ForbidDangerousFunctionsRule` as the unconditional floor for its ten functions, and
adopt spaze's execution, dangerous and insecure bundles for the coverage we do not have at all
(weak hashing and randomness, unparametrized SQL calls, superglobals, the backtick operator).
Per the Owner's standing ruling, overlap between two engines is acceptable when kept in sync and
documented; a test will assert every function our rule names is still named by it, so the floor
cannot be removed by accident.

**Task 3.2, shipmonk/dead-code-detector: adopt opt-in, default off, and measure before enabling
it here.**

The stated precondition is met — the 10:55 probe confirmed the PHAR loads Composer-installed
extensions. The real question was the false-positive surface on a library, and the answer is
that there is exactly one knob: `@api` phpdoc on a class, interface or method marks it an
entrypoint. There is no "this is a library" switch. Without that annotation every public method
a consumer calls but `src/` does not would be reported dead, and `usageExcluders.tests.enabled`
— which the package recommends for all projects — makes it worse for us, because it stops our
own test suite from counting as usage.

That is not a reason to reject it; it is a reason not to switch it on blind. The 18 pipeline
types retagged `@api` earlier today are the start of the surface it would need. The task is
therefore: ship it behind `withDeadCodeDetection(bool)` defaulting to false, then run it over
this repository as a measurement and record the count of reports and how many are genuine before
deciding whether this repository turns it on. Its `--error-format removeDeadCode` autofix stays
out of the pipeline; on a library with an incomplete `@api` surface it deletes public API.

## 12:05 · action · — — Task 2.1 and 2.2: the analyser lane, and what it caught immediately

`ComposerDependencyAnalyserTool` runs in the linting phase directly after
`composerRequireChecker`, alias `cda`, identifier `phpqaci.composerDependencyAnalyser`, config
resolved as `composer-dependency-analyser.php` through the usual three levels. The package went
into `require`, not `require-dev`: php-qa-ci's `require-dev` is not installed for consumers, so
a tool a consumer's pipeline must run has to be a hard requirement. A missing binary fails the
lane rather than skipping it — the phploc lesson from 11:10, where a lane that skips silently
stayed dead for years.

### Task 2.2, the evaluation, answered by the first run rather than by argument

composer-require-checker reports **nothing** on this repository: "There were no unknown symbols
found." The analyser, on the same manifest, reported 36 unknown classes and **12 unused
dependencies**, two of which were real:

| Package | Section | Verdict |
| --- | --- | --- |
| `sqlftw/sqlftw` | `require` | Added in commit `a2aa93d`, which touched **only** composer.json and composer.lock. Never used, by anything, ever. |
| `funkyproject/reflection-file` | `require` | Last referenced before `e3341df`; the usage went and the declaration stayed. |

Both were in `require`, so every consumer of php-qa-ci has been installing them — five packages
once transitives are counted — for no reason. Neither could ever have been found by
composer-require-checker, and the reason is structural rather than a gap in its configuration:
it traces symbols to packages, so a package that contributes no symbol contributes no evidence.
An unused dependency is invisible to it by construction.

That settles the recommendation the plan asked for: the two are **complements, not substitutes**,
and neither is a candidate to replace the other. Require Checker answers "what should have been
declared"; the analyser answers "what is earning its place". This repository passed one and
failed the other on the same day.

Following the method, the lane was built and shown reporting through `bin/qa -t cda` before
anything was removed; the removal came after.

### Configuring the remainder

The 36 unknown classes were almost all the vendoring strategy working as designed: `PHPStan\*`
comes from `vendor-phar/phpstan.phar` and is `replace`d in composer.json, `Arkitect\*` exists
only as a PHAR, `Composer\*` is supplied to a plugin at run time by Composer itself. Those, plus
the deliberately unautoloadable fixture trees under `tests/assets`, are declared in
`qaConfig/composer-dependency-analyser.php` with the reason for each.

Worth recording as a property of the tool: **an ignore that matches nothing is itself reported**.
The first version of that config had three entries the tool told me were never applied, because
a later regex already covered them. That is the opposite of how a baseline behaves, and it means
this config cannot quietly rot into a list of stale excuses.

Final state: 282 files scanned, zero findings, lane green.

## 12:30 · finding · joseph — DBF 3.1 record: an array parameter that should be variadic

**Originating instance**, spotted by the Owner in review:

```php
/** @param list<string> $baseArgs */
private function runText(ToolContext $context, string $logDir, array $baseArgs): ToolResultDto
```

Owner's words: "use splat when you can to guarantee typed array".

**Class**: a method's last parameter is declared `array` and its element type exists only in a
`@param list<T>` docblock, where the signature could instead be `T ...$name`.

**Hazard**: the element type is a promise, not a check. Two things follow. The engine performs
no check at all at run time, so the guarantee holds only where PHPStan runs and can see the
call — which excludes a consumer project's `qaConfig/tools/*.php` override calling into this
library, the case php-qa-ci exists to serve. And the promise lives in a docblock, so it can
drift from the signature silently; a variadic cannot, because there is nothing to drift from.

**Honest limit on the hazard, recorded because it bounds how much this is worth**: at level max
PHPStan already rejects passing `array<mixed>` or `mixed` where `list<string>` is documented, so
for a *private* method whose callers are in the same file this buys little beyond deleting a
docblock. The real gain is at a boundary PHPStan does not analyse. The class is worth enforcing
anyway — consistency is the point, and the weaker cases cost nothing to convert — but this is a
convention, not a bug class, and the record should not pretend otherwise.

**Technique 1 — text search.** `grep -rn '@param list<\|@param array<' src/` returns 82
docblocks; pairing each with the following `function` line gives 13 signatures.

**Technique 2 — structural AST scan** (`untracked/scratch/variadic-scan.php`): every method whose
last parameter is declared `array`, read from the parse tree with no reference to any docblock.
It found **39**, including several the text search missed because the signature spans multiple
lines and the `@param` is not on the adjacent line — `SealingAttributeReader::factoryFor` and
`BranchNamePolicyDecision::decide` among them. The techniques are independent in the required
sense: technique 2 found instances technique 1 could not, and technique 2 needs no docblock to
exist at all.

Breakdown of the 39: 1 by-reference, 13 promoted constructor properties, 9 maps, **16
list-typed candidates**.

The 9 "no docblock" hits the scan first reported were a defect in the scan, not in the code:
its `@param` regex was `([^\s]+)`, which stops at the space in `array<int|string, mixed>`. All
nine are maps. That there are no genuinely undocumented array parameters is consistent with
level max plus strict rules requiring an iterable value type.

**Bounding the class narrower — each exclusion with the reason it cannot carry the hazard:**

1. **Promoted constructor properties** (13). PHP forbids promoting a variadic parameter, so the
   alternative does not exist.
2. **By-reference parameters** (1). A variadic receives copies; converting changes semantics.
3. **Maps, `array<K, V>` with a key type** (9). A variadic is always a list; a map has no
   variadic spelling.
4. **Constructors** (3 of the 16: `FactorySealedRule`, `ForbidDeprecatedPhpunitMethodRule`,
   `RequireSensitiveParameterAttributeRule`). PHPStan builds rule objects from the neon DI
   container, which passes each `arguments:` entry as one positional value; a variadic
   constructor would silently re-interpret an array argument as many.
5. **Methods carrying `#[DataProvider]`** (1: `testPhaseOrderMatchesGolden`). PHPUnit passes
   each provider row's elements as separate arguments, so a `list<string>` element is one
   argument. Made variadic, the row's meaning changes without any error.
6. **Methods that implement an interface or override a parent.** The signature is fixed by the
   supertype and cannot be changed from the subtype alone.

**Bounding the class wider**: the rule must not catch only `PhpstanTool`. After the exclusions
above, **12 instances** remain across 9 files in `src/` and `tests/`, which is the class the
rule must cover. No wider rule is deferred: parameters in a non-final position cannot be
variadic at all, so there is nothing wider to build.

**Runner check**: the behaviour is pinned by the rule's own fixture-based test in the shape of
the other bundled rules, not by a behavioural test of the converted call sites — converting a
parameter to a variadic changes no observable behaviour, so there is nothing behavioural to pin.

## 12:40 · finding · — — a cost of the variadic class, recorded before the sweep

`BranchNamePolicyDecision::decide(string $branch, array $exemptBranches, array $allowedPrefixes)`
takes **two** list parameters. Only the last can become variadic, so the conversion leaves two
conceptually identical parameters spelled differently — one `array $exemptBranches`, one
`string ...$allowedPrefixes`.

This is not a false report: the hazard is real for `$allowedPrefixes`, and converting it does
place that one under engine enforcement. But it is an honest cost of the class, and the rule
cannot avoid it, because "convert both" is not available — PHP allows one variadic and only in
final position. Recorded so the asymmetry in that signature reads as a known consequence rather
than an oversight.

No narrowing added for it. A narrowing exists to exclude code that does not carry the hazard,
and this code does.

## 12:55 · finding · — — twig-cs-fixer's documented signing key is the wrong one

Task 2.5 needs twig-cs-fixer's PHAR through PHIVE, which means adding its signing key to
`TRUSTED_KEYS` in `scripts/tool-install.bash`. That list is a security boundary, so the key was
verified rather than taken from the reconnaissance report.

The report quoted the upstream README: "The PHAR files are signed with a public key which can be
queried at `keys.openpgp.org` with the id `AC0E7FD8858D80003AA88FF8DEBB71EDE9601234`". Two
checks contradict it:

- `phive install twig-cs-fixer` reads the key id out of the `.asc` signature itself and fetched
  **`6371 FDC5 34E4 7BD9 7920 8B6F 21A1 0B2F 4F04 88C9`**, "Vincent Langlet
  <mr.vincent.langlet+gpg@gmail.com>", created 2024-08-13.
- `keys.openpgp.org` returns **404, "No key found for fingerprint"** for the README's
  fingerprint. The documented key does not exist there to be queried.

So the verification procedure the project documents cannot be carried out by anyone following it.

**The explanation is key rotation, not compromise.** Reading the key id out of the detached
signature of several releases in turn:

| Release | Signing key id |
| --- | --- |
| 3.0.0 | `DEBB71EDE9601234` |
| 3.6.0 | `21A10B2F4F0488C9` |
| 4.0.0 | `21A10B2F4F0488C9` |
| 4.1.1 | `21A10B2F4F0488C9` |

`DEBB71EDE9601234` is the last sixteen hex digits of the README's fingerprint. The README
documents the key that signed 3.0.0; the maintainer rotated to a new key before 3.6.0 and the
README was never updated, and the retired key has since gone from the keyserver — which is why
it 404s rather than merely mismatching.

**Consequence for us**: the key to trust is `6371FDC534E47BD979208B6F21A10B2F4F0488C9`,
corroborated by being the same key across three consecutive releases and by its identity
matching the maintainer, not by the project's own documentation, which is stale. The trusted-key
entry records that reasoning so nobody later "corrects" it to the README's value. Worth an
upstream issue.

Recorded as a lesson about the reconnaissance too: the agent reported the README's claim
accurately and the README was wrong. A quoted document is evidence of what the document says,
not of the fact it asserts — for a key fingerprint the artefact is the only authority.

## 13:20 · finding · — — type-coverage would have been silently inert in this pipeline

Task 2.3 wired `tomasvotruba/type-coverage` through the phpstan lane behind
`withTypeCoverageFloors()`. Verifying it end-to-end rather than trusting the wiring found that
the extension reported **nothing at all**, while the same code under level max reported the
`missingType.*` errors as usual — so the extension was loading and doing nothing.

The cause is in the package's own guard. Every one of its five rules begins:

```php
if (! ScopeConfigurationResolver::areFullPathsAnalysed($scope)) {
    return [];
}
```

and that resolver reaches into PHPStan's container to compare `analysedPaths` with
`analysedPathsFromConfig`. They are equal only when the paths came from the configuration file.
The phpstan lane has always passed the paths as command-line arguments
(`analyse src tests -c wrapper.neon`), which makes those two differ on **every** run, so the
guard is tripped every time. The reasoning behind the guard is sound: a coverage percentage
measured over a subset of the project is a misleading number.

Had this shipped as written, a project could have set floors, watched the pipeline go green and
concluded its type coverage met them, when nothing was ever measured. That is worse than not
having the feature.

**Fix**: when the floors are on *and* the whole project is being analysed, the lane now writes
`parameters.paths` into the generated wrapper neon and passes no path argument, so the two agree
and the rules run. A `-p` run keeps the command-line form deliberately, which leaves
type-coverage correctly silent for that run rather than reporting a percentage of a subset.
Both behaviours are pinned by tests.

**A correction to the 11:40 reconnaissance.** It reported that only `DeclareCoverageRule` sets a
PHPStan error identifier and that the other four emit none. Reading the installed 2.3.6 source,
all five do:

`typeCoverage.returnTypeCoverage`, `typeCoverage.paramTypeCoverage`,
`typeCoverage.propertyTypeCoverage`, `typeCoverage.constantTypeCoverage`,
`typeCoverage.declareCoverage`

all confirmed firing in a probe run. So the constraint recorded at 11:40 — that a consumer could
only suppress these by matching message text, which `phpstanIgnoreJustification` cannot tie to an
identifier — does not exist. The identifiers are stable and documentable, and Task 2.3's original
requirement stands unmodified.

## 13:45 · action · — — DBF 3.3 and 3.4 for the variadic class

`RequireVariadicOverArrayParameterRule`, identifier `phpqaci.variadicOverArrayParameter`,
registered in `rules-optional.neon`. Built by the `php-qa-ci_phpstan-rule-creator` agent to the
class recorded at 12:30, then corrected in two places before it could be trusted.

**Correction 1, the supertype exclusion did not fire.** Its fixture put an interface, its
implementation, an abstract parent and its child in one file. `ClassReflection::getParents()`
and `getInterfaces()` resolve through the ReflectionProvider, which cannot find a class whose
file does not match it under PSR-4, so every ancestor lookup silently returned nothing and the
implementations were reported alongside the declarations. Splitting the fixture into one class
per file fixed it. Worth noting for future rules: an exclusion that depends on reflection cannot
be tested from a multi-class fixture file, and it fails *open* — reporting more, not less.

**Correction 2, the suggested declaration did not parse.** `list<class-string>` produced
`declare it "class-string ...$sealingAttributes"`, and `class-string` has no native spelling.
A message naming an invalid declaration is worse than no message. The rule now maps a docblock
refinement to its native base — `class-string` and `non-empty-string` to `string`, `int<0,max>`
and `positive-int` to `int` — so the signature is native and the refinement stays in the
docblock, which is the honest split: the engine checks the base, the docblock keeps the detail.

**3.3, the rule fires on the originating instance**, through the project's own harness:

```
$ bin/phpstan-rule phpqaci.variadicOverArrayParameter src
phpqaci.variadicOverArrayParameter FIRED (9)
/workspace/src/Pipeline/Lane/PhpstanTool.php:95  Parameter $baseArgs of runJson() ...
/workspace/src/Pipeline/Lane/PhpstanTool.php:125 Parameter $baseArgs of runText() ...
```

**3.4, the sweep, recorded before any instance is fixed: 12 — 9 in `src/`, 3 in `tests/`.**

| File | Method | Documented type |
| --- | --- | --- |
| `ConfigTemplateIgnoreList/ConfigTemplateIgnoreListAuditor.php:100` | `isCovered` | `list<string>` |
| `Markdown/LinksChecker.php:318` | `getLastStatusCode` | `array<string>` |
| `PHPStan/Rules/ForbidSilentCatchRule.php:92` | `blockContainsThrow` | `array<Stmt>` |
| `PHPStan/Rules/ForbidSilentCatchRule.php:114` | `blockContainsLogging` | `array<Stmt>` |
| `PHPStan/Rules/RequireConsistentMemberDocsRule.php:84` | `check` | `list<ClassConst\|EnumCase\|Property\|Param>` |
| `PHPStan/Rules/SealingAttributeReader.php:33` | `factoryFor` | `list<class-string>` |
| `Pipeline/Lane/BranchNamePolicy/BranchNamePolicyDecision.php:24` | `decide` | `list<string>` |
| `Pipeline/Lane/PhpstanTool.php:95` | `runJson` | `list<string>` |
| `Pipeline/Lane/PhpstanTool.php:125` | `runText` | `list<string>` |
| `tests/.../ForbidRawSqlRuleTest.php:116` | `methodCall` | `list<Arg\|VariadicPlaceholder>` |
| `tests/.../RequireReadonlyServiceRuleTest.php:139` | `class` | `list<\PhpParser\Node\Stmt>` |
| `tests/.../Phpunit/PhpunitArgumentsTest.php:119` | `build` | `list<string>` |

**The rule's count matches the 12:30 prediction exactly**, which is the cross-check that
matters: the class was bounded by two searches and a set of written exclusions before the rule
existed, and the rule independently arrived at the same twelve. Neither the searches nor the
rule found anything the other missed.

## 14:30 · action · — — DBF 3.4 closed: 12 of 12 converted, sweep at zero

```
$ bin/phpstan-rule phpqaci.variadicOverArrayParameter src   → did not fire
$ bin/phpstan-rule phpqaci.variadicOverArrayParameter tests → did not fire
```

Suite at 827 tests, `allStatic` green, `allLints` green. Two agents on deliberately disjoint
file sets, after the earlier failure in this plan where concurrent agents doing text replace-all
corrupted eight shared files.

**The pipeline caught four defects in this batch of my own work**, which is the point of running
it against itself:

- `phpqaci.consistentMemberDocs` on `TwigCsFixerTool` — I had documented one constant of four.
  The comment moved into the class docblock, where it belonged.
- `phpqaci.repeatedStringLiteral` on two new test classes.
- `phpqaci.nullCoalescingEmptyString` on my own `?? ''` in the new rule, where an explicit
  `isset()` was the right shape.
- `argument.type` on the three spread sites in `PhpstanTool`: a variadic parameter is
  `array<int<0,max>|string, string>` to PHPStan, not `list<string>`, because a caller may pass
  named arguments. Re-spreading one into a `list<string>` parameter therefore needs
  `array_values()`. **Worth knowing before converting anything else to a variadic**: the
  variadic guarantees the element type, which is the point, but it does not guarantee list-ness
  inside the callee.

## 14:35 · decision · joseph — Phase 3 rulings

**Task 3.1, spaze/phpstan-disallowed-calls: do not take the dependency; lift and shift the
deny-lists into our own rule.** Owner's reasoning: "so we can fine tune to provide a coherent
ruleset without risk of drift and conflict". That answers the concern recorded at 11:45 more
directly than adopting alongside would have — there is no second engine to drift from, and no
`allowIn` escape hatch outside what `phpstanIgnoreJustification` can audit, because we are
taking the lists and not the engine.

Licence checked as instructed: **MIT**, confirmed from the repository's own `LICENSE` file
(Copyright (c) 2018 Michal Špaček), not merely from the Packagist metadata. Lifting is permitted
with attribution, which the provenance comments the Owner asked for provide.

**Task 3.2, shipmonk/dead-code-detector: do not bundle it. Dogfood it first through a
project-level extended pipeline, and make that process the documented archetype.** Owner's
words: "we can always install a QA tool and dog food it against ourselves before bundling it in
the full pipeline / php-qa-ci can/should allow projects to extend the pipeline with extra tools
etc / make this the archetype - document the process clearly".

Extended in a follow-up message to cover tool **groups** as well: a project should be able to
register extra tools and extra groups, place a tool in a new or existing group, and control
group execution order, "with well justified defaults that people are unlikely to change".

This is a capability the pipeline does not currently have — see the next entry.

## 15:10 · finding · — — a rule for the variadic class already existed, and DBF 3.1 did not ask

`RequireVariadicForSingleListParamRule`, identifier `phpqaci.arrayListShouldBeVariadic`, has
been registered in `rules-optional.neon` since commit `7da2390`. It enforces the same convention
the Owner raised at 12:30, and I had an agent build `RequireVariadicOverArrayParameterRule`
alongside it: two rules for one convention, in the same engine, which is the duplication this
repository's own guidance exists to prevent.

**Why the method did not catch it.** Clause 3.1 requires two independent searches, and both were
run properly — but both search for *instances of the defect*. Neither asks whether a *defence
already exists* for the class. An AST scan for `array` parameters and a grep for `@param list<`
will both return zero hits from a rule file, because a rule contains no instances of what it
forbids. The gap is structural, not carelessness: **3.1 needs a third question — does a rule
already cover this class? — and `bin/rules` answers it in one command.** Recorded as a method
lesson to raise against the specification.

**The resolution is a merge, not a coin toss**, because the new rule is a strict improvement:

| | `arrayListShouldBeVariadic` | `variadicOverArrayParameter` |
| --- | --- | --- |
| Scope | methods with exactly one parameter | the last parameter of any method |
| Exclusions | none | six, each with a recorded reason |
| Constructors | reports them, which would break PHPStan's own neon DI wiring | excluded |
| `#[DataProvider]` methods | reports them, which would silently re-interpret provider rows | excluded |
| Interface implementations | reports them, though the signature belongs to the supertype | excluded |

The old rule is narrower *and* carries false positives the new one deliberately avoids. It
passes on this repository today only because "exactly one parameter" happens to match nothing
here — not because it is correct.

**Recommendation: delete `RequireVariadicForSingleListParamRule` and keep the new rule.** Coverage
strictly increases and three classes of false positive go away. Deleting a rule and retiring a
published identifier is Owner-only, so this is recorded and not acted on.

## 15:15 · decision · joseph — the pipeline becomes a construction, not a hardcoded list

Follow-up ruling on Task 3.2's archetype: "defaults can be enums, but ... composition over
inheritance / a pipeline builder that can start from default and extend it or can fully
lobotomise the pipeline and build from scratch if wanted / default tools can be provided by
enums etc for very strict typing - but the actual pipeline is a construction not hard coded".

Recorded for plan 00005. The shape follows `QaConfigBuilder`, which is already immutable withers
over a DTO: `PipelineBuilder::defaults()` seeded from the enums, `::empty()` for the
build-from-scratch case, and withers for extra phases, extra tools and phase order. The enums
stay as the strictly-typed *catalogue*; the pipeline stops being that catalogue and becomes a
value built from it.

What that has to move, recorded now while it is fresh:

- `PhaseEnum` is currently both the catalogue *and* the execution order, since `Pipeline` walks
  its declaration order. Those two roles have to separate.
- `ToolRegistry::shipped()` is a frozen literal and `Pipeline` iterates `toolsForPhase()`, so a
  project tool never runs. `ShippedToolLocator` will already *find* `qaConfig/tools/<name>.php`
  for any name — the registry is the only thing stopping an extra tool, which makes this a
  smaller change than it looks.
- `ToolDefinitionDto` is `@internal` and would have to become `@api`, since consumers would
  construct one.
- `ToolRegistryCharacterisationTest` holds frozen goldens for the alias map, phase order and
  path classification. Those are the guard that the *default* pipeline does not drift, and they
  stay — they just become assertions about `PipelineBuilder::defaults()` rather than about a
  hardcoded list.

## 15:40 · action · — — the spaze lift, and what it caught here

Licence verified from the upstream `LICENSE` file rather than the Packagist metadata: **MIT,
Copyright (c) 2018 Michal Špaček**. Lifting is permitted with attribution, which the class
docblocks and the two remediation pages provide.

The lists are split by hazard rather than dropped in wholesale, because "dangerous" was covering
three different things:

| Rule | Bundle lifted | On by default? |
| --- | --- | --- |
| `ForbidDangerousFunctionsRule` (existing, extended) | execution-calls, plus the source-disclosure entries of dangerous-calls | **yes**, as before |
| `ForbidInsecureFunctionsRule` (new) | insecure-calls | no, opt-in |
| `ForbidDebugOutputFunctionsRule` (new) | the dump functions of dangerous-calls | no, opt-in |

The always-on rule gained `pcntl_exec`, `create_function`, `dl`, `highlight_file`, `show_source`
and `phpinfo` — all things with no legitimate place in application source, and none of them used
anywhere here, so consumers upgrading are unlikely to be surprised. It also stopped emitting one
generic message: each function now carries its own reason and its own alternative.

**Deliberately not lifted, with the reason**, since a lift is a choice and the omissions should
be as visible as the inclusions:

- `putenv` — the pipeline itself calls it legitimately (`QaApplication` sets `XDEBUG_MODE` for
  the coverage run). Banning something we do correctly ourselves is how a rule loses authority.
- `apache_setenv`, `pfsockopen`, `proc_nice`, the `posix_*` and `socket_*` families — these are
  environment-specific rather than wrong. A daemon or CLI tool may need them. Candidates for a
  future opt-in bundle, not for a default ban.

**The exemption that justifies these being rules rather than a grep** is carried across intact:
`print_r($x, true)` and `var_export($x, true)` return a string instead of printing, so they are
not reported. That is not hypothetical — both of this repository's own `var_export` calls use
that form, and the rule correctly leaves both alone.

**What the new rules found on this repository: four real instances, all fixed rather than
suppressed.**

| Site | Was | Now |
| --- | --- | --- |
| `DeploySkillsCharacterisationTest:615` | `sha1()` fingerprint | `hash('sha256', ...)` |
| `LinksCheckerTest:204` | `uniqid('', true)` temp dir | `bin2hex(random_bytes(8))` |
| `ManagedSourceGeneratorTest:31` | `uniqid()` temp dir | `bin2hex(random_bytes(8))` |
| `RedirectStubHooksDaemonNoticeTest:39` | `uniqid('', true)` temp dir | `bin2hex(random_bytes(8))` |

None was a security defect, which is exactly why the rule is opt-in. Three of them are a real
improvement anyway: `uniqid` is time-based and collides under parallel test runs, which is the
one thing a temp directory name must not do.

`phpqaci.consistentMemberDocs` also caught all three rule classes at once — I had documented the
function-list constant and not `IDENTIFIER`. Per the standing ruling the free text moved into
the class docblock and the `@var` tag stayed, since a tag-only docblock does not count as
documentation.

Suite 847 green, `allStatic` and `allLints` green.
