# Plan 00006 — Journal 26-09-10

> Append-only activity log for plan 00006; entry grammar as in the 26-09-09 file.

## 06:40 · action · T3.3 — consumers moved, all tasks done

Owner pushed php8.5 (f9ced1a..d0a6573). accounts-api: lock to d0a6573, allow-plugins entry
gone, the analyser config's shadow-dependency self-ignore removed (the qaConfig unknown-class
ignore now covers the file); lock shrank by roughly 900 lines as the tools' transitive packages
left; full pipeline ALL TESTS PASSING, committed 5427de7 and pushed. accountsiq on its chore
branch: same three changes; first full run failed on exactly that stale self-ignore ("was
ignored ... but it was never applied"), second run ALL TESTS PASSING; committed fc98c43, pushed
to chore/php85-and-php-qa-ci-php85 (PR #46). Clone returned to main 6d26b75 so accounts-api's
lock reference matches.

Every task and success criterion is ticked. Marking the plan Complete and moving it to
Completed/ is the Owner's call.
