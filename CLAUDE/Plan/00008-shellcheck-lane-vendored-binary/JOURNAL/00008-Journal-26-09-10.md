# Plan 00008 — Journal 26-09-10

> **Append-only activity log** for plan 00008. One file per day
> (`00008-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:11 · action · — — plan scaffolded

Plan 00008 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:35 · finding · T3.1 — the branch-rule risk was imaginary

The plan's second-highest risk was "deleting the CI job while the branch rule still
requires its check", and I had told the owner the rule listed `shellcheck` as required.
It does not. `repos/LongTermSupport/php-qa-ci/rules/branches/php8.5` resolves to one
ruleset (18472656) carrying `deletion`, `non_fast_forward` and `pull_request` — there is
no `required_status_checks` rule at all, and no classic branch protection either (the
protection endpoint 404s). So nothing was ever blocking on the job's context and the
deletion needs no paired rule change.

Recorded because the claim was stated to the owner as fact before it was checked, and a
required-check answer is one API call away.

## 14:40 · finding · T3.2 — discovery reached 72 files where CI checked 29

First run of the lane over php-qa-ci itself, exactly the consequence Decision 3 predicted:
one finding, in a file the old CI invocation never looked at.

```
In .claude/hooks/test-all-hooks.sh line 2:
cd /var/www/vhosts/green/checkout/vendor/lts/php-qa-ci/.claude/hooks
^-- SC2164 (warning): Use 'cd ... || exit' or 'cd ... || return' in case cd fails.
```

The SC2164 was the lesser half of it. That absolute path only ever matched one machine,
so the script was already broken everywhere else — `cd` to `dirname "${BASH_SOURCE[0]}"`
fixes the real defect and the finding together.

Equivalence for Task 3.2, both on the merged tree: CI's exact old invocation exits 0, the
lane exits 0 over 72 files. Known-positive holds — `git show 67bd110^:scripts/build-phar.bash`
fails on SC2034, the fixed file passes.

## 14:55 · decision · T2.1 — the exec bit, which the PHARs never had to care about

Nearly shipped a lane that breaks for anyone installing from a Composer dist zip.
`ZipArchive::extractTo` does not restore permissions, so a dist-installed
`vendor-bin/shellcheck` arrives mode 0644. Every existing vendored artefact is a PHAR
handed to `php`, so this class of failure has never come up here.

`scripts/tool-install.bash` cannot cover it: its `post-install-cmd` fires for php-qa-ci's
own install, not a consumer's. So `ShellCheckBinary::ensureExecutable()` repairs the mode
at lane start and returns the reason when it cannot, which the lane prints.

## 15:05 · finding · P2 — PHPStan found 27, and three were real

The house rules did most of the work. Worth keeping:

- `variadicOverArrayParameter` forced the trailing `list<string>` parameters to be real
  variadics, which incidentally made the test call sites read better
  (`$this->find(self::BUILD, 'scripts/deleted.bash')`).
- `arrayFilter.strict` caught `array_filter()` with no callback dropping `"0"` as well as
  `""` — a tracked file literally named `0` would have been silently unchecked.
- The catch-block rule rejected swallowing `FilesystemException` from `\Safe\chmod`.
  Returning `?string` instead of `bool` means the operator is told *why* the mode could
  not be set, which is strictly better than the boolean I first wrote.

Two `Safe\preg_match` uses went away entirely rather than being guarded: the shebang and
the version banner are both easier to read as `str_starts_with` plus `substr`, with no
`$matches[1] ?? ''` to reason about.

## 15:20 · finding · — — the same nineteen risky tests as Plan 00007, for the same reason

First full pipeline: exit 1, nineteen risky tests, every one of them
"executed code that is not listed as code to be covered or used:
`LTS\PHPQA\Pipeline\Lane\ShellCheckTool`". Identical to the trap Plan 00007 hit with
`OpcacheTool`, in the same five files, because they all walk the whole shipped set.

The unit suite is green either way — `--fail-on-risky` only comes from the pipeline's own
phpunit invocation. That is the second time this has cost a pipeline run, so it is now a
bullet in `CLAUDE/tool-boundaries.md` under "Before adding one" rather than something to
rediscover a third time.

## 15:25 · finding · — — both consumers preview clean

Ran the vendored binary over each consumer's git-tracked shell surface with the lane's
own discovery rule, before recommending the lock move: accounts-api 44 candidates, exit 0;
the accountsiq clone 43 candidates, exit 0. So moving either lock to this tip does not
land a red lane on them.

## 15:45 · finding · T3.1 — correction: the required-checks risk is real after all

The 14:35 entry is wrong and this supersedes it. The owner's push of `php8.5` printed:

```
remote: Bypassed rule violations for refs/heads/php8.5:
remote: - Changes must be made through a pull request.
remote: - 2 of 2 required status checks are expected.
```

So something does require two status checks, and `ci.yml` had exactly two jobs —
`ShellCheck (severity=warning)` and `QA Pipeline`. Deleting the first leaves a required
context that never reports, which blocks every PR for anyone without bypass.

Why the earlier check missed it: `repos/LongTermSupport/php-qa-ci/rules/branches/php8.5`
returns only `deletion`, `non_fast_forward` and `pull_request`, and
`repos/.../rulesets?includes_parents=true` lists one repo ruleset. The status-check rule
must therefore live in an org-level ruleset, and `orgs/LongTermSupport/rulesets` needs the
`admin:org` scope this token does not have — it 404s. A negative from an endpoint that
cannot see the whole picture is not a negative, and I reported it as one.

The deletion stands; the rule change is owner-held and is now a box on Task 3.1.

## 15:55 · decision · T1.2 — the updater moved from Bash to PHP mid-build

Owner ruling, mid-task: real scripting should be PHP wherever possible. The ~90 lines of
Bash I had just written and proved were exactly what the ruling is about — a release
lookup, a JSON parse, an archive unpack and a five-way decision, none of it visible to a
test or to PHPStan. Untested Bash logic inside a tool that exists to catch untested logic.

Now: `InstallDecider` is a pure decision table with a test per branch (nine of them,
including the two the Bash never handled — an unpinned checkout, and a current pin whose
binary has gone missing, which the Bash would have reported as up to date). The I/O is
`ShellCheckInstaller`; the entry point is `bin/shellcheck-install`; `tool-install.bash`
keeps one `php` call and the token borrow.

Nothing is shelled out any more. Switching the asset from `.tar.xz` to `.tar.gz` is what
buys that: `PharData` reads gzip, so `curl` and `tar` both go. It costs 1.2 MB of download
on a maintainer-only path and removes two host dependencies.

The project's own guards did the review. `ComposerBinManifestTest` caught the new
executable missing from composer.json's `bin`; the catch-block rule rejected two silent
`catch (Throwable)`s, and turning them into printed causes means a rate limit no longer
looks like an empty release list. The ruling is recorded in `CLAUDE.md`, not just here,
since it governs the next script anyone writes.

## 16:00 · handoff · P1,P2,P3 — all three phases delivered

Every task box ticked bar the owner-held required-checks change, and every success
criterion met. The owner has pushed `php8.5` up to the plan-doc commit `722c57f`; the
implementation commit is still local. The two consumer locks move to the new tip after
that, and both preview clean.

## 18:40 · decision · T3.1 — correction: fix the rule, do not build a job to satisfy it

I read the owner's "do not remove, fix" as "make the stale context report again", and added a
second CI job named `ShellCheck (severity=warning)` running `bin/qa -t shellCheck`. That was wrong
and is reverted.

The owner's actual point: **fix the branch rules, do not contort the workflow.** The whole pipeline
is the check — ShellCheck is a lane inside `bin/qa`, and `QA Pipeline` already covers it. A job
that exists only to publish a context is the second mechanism this plan was written to delete,
bought at the price of a duplicate `composer install` on every CI run, to satisfy a rule that is
simply out of date.

What "fix" means here is: correct the required-checks list to name the check that exists,
`QA Pipeline`. That is a ruleset edit, not a workflow edit, and it stays owner-held because the
rule is org-level and needs `admin:org`.

The general lesson, and the second time this plan has produced it: when a rule and the world
disagree, check which one is wrong before making the world match the rule.
