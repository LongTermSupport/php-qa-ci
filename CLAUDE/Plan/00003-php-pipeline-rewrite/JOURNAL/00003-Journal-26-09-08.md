# Plan 00003 — Journal 2026-09-08

> **Append-only activity log** for plan 00003. One file per day
> (`00003-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 22:05 · action · — — plan scaffolded

Plan 00003 created via `mkplan.bash`; `JOURNAL/` initialised. Owner's brief: the php8.5
branch is not in use anywhere, so the Bash-to-PHP rewrite ships with the 8.5 switch; no
BC with the Bash consumer contract; all real functionality TDD PHP 8.5 at PHPStan max.

## 22:06 · decision · — — failsafe recovery cron

Session failsafe recovery cron id `13391032` (hourly at :23, session-only). One per
session; reuse it, do not create another.

## 22:08 · decision · P1 — architecture

Recorded in PLAN.md Technical Decisions: PHP `bin/qa` (symfony/console), in-process
checks called directly, typed `qaConfig/qa.php` builder, env var names kept, timing/ETA
dropped. Symfony 8.1 (process, console) available for PHP 8.5. nette/neon decodes the
flat `branchNamePolicy.yaml` lists, so no YAML dependency is needed.

## 22:40 · action · P1 — foundations landed

Config, Tool, Process and Lock namespaces written test-first (196 Small tests, PHPStan max
clean). Two rule-driven design choices worth knowing:

- `QaConfigBuilder` is `final readonly` with withers, not a mutable builder: the opt-in
  `RequireReadonlyServiceRule` (enabled in this repo's self-check) flags any `final` non-readonly
  class that is not in its exception list, and "Builder" is not. A consumer's `qaConfig/qa.php`
  therefore returns the adjusted builder rather than mutating one.
- DTOs live under a `Dto\` segment with a `*Dto` suffix (arkitect default tier), so
  `ProjectPathsDto`, `QaConfigDto`, `ToolDefinitionDto`, `ToolResultDto`, `ProcessSpecDto`,
  `ProcessResultDto`, `LockInfoDto`.

The comment-size guard misreads a `'/*'` glob literal in PHP as an opening comment; tests use
`TempDir` (tests/Support) and `scandir` instead of `glob($dir . '/*')`.

## 23:05 · action · P1 — runner and CLI landed

`Pipeline`, `ToolExecutor` (retry policy), `AggregateReport`, `DirectoryPreparer`,
`PharToolsVerifier`, `HookRunner`, `ShippedToolLocator`, `ProjectConfigLoader`,
`ArgumentsParser`, `PathNormaliser`, `QaApplication` and `bin/qa-php`. 254 Small + Large
tests, PHPStan max clean over `src/Pipeline`, `tests/Small/Pipeline`, `tests/Large/Pipeline`,
`tests/Support`, `bin/qa-php`.

Decisions taken here:

- `bin/qa-php` is a temporary name: the Bash `bin/qa` stays the shipped command until every
  lane is ported (Phase 5 renames). Both are in composer.json `bin` and the `.gitignore`
  allow-list meanwhile.
- The end-to-end test proves the legacy guard: this repository still has
  `qaConfig/qaConfig.inc.bash` for the Bash pipeline, so the PHP entrypoint stops there with
  the migration message. Phase 4 replaces the file with `qa.php` and the assertion with a real
  single-tool run.
- The Symfony `service-contracts` package arrived with symfony/console; Rector then rewrote a
  `ResetInterface` class-name string to `::class` in `RequireReadonlyServiceRule`. Legitimate
  autofix, committed as such.
