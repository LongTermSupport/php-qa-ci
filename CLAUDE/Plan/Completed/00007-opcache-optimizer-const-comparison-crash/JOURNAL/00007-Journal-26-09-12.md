# Plan 00007 — Journal 26-09-12

> **Append-only activity log** for plan 00007. One file per day
> (`00007-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 09:05 · decision · T3.1 — the release is not a task; the drift is a defect

The one open item was "when a fix ships, set `FIRST_FIXED`". PR 23648 is still open, so
the release does not exist, and a plan held open to wait for an event outside the
repository is a reminder pretending to be work. The Owner asked for the blocker resolved.

Reframed under the method: the hazard is not "the constant is unset", it is "the constant
disagrees with the PHP in use" — the lane and the start-up advisory firing on a fixed PHP,
or staying silent on an exposed one. That is a class with a detector.

`tests/Large/Opcache/OpcacheDefectRangeTest.php` compiles the six-line shape filed
upstream through the running PHP's optimizer with exactly the lane's ini (compile only —
executing it segfaults on an affected PHP), parses the dump with the lane's own
`DumpParser`, and requires the two-constant-operand fingerprint to be present **iff**
`OpcacheDefects::affects(PHP_VERSION)`. On this 8.5.10 the affected branch ran and the
fingerprint was there. On the first fixed PHP the test fails naming the version to set.

Fixture: `tests/assets/opcache/gh23644-shape.php`, header stating it is never executed.

## 09:06 · handoff · — — closed

Every task done; the one-line `FIRST_FIXED` edit is now demanded by the build rather than
remembered by a plan. Moved to `Completed/`.
