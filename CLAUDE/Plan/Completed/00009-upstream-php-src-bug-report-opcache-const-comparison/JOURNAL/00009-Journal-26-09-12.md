# Plan 00009 — Journal 26-09-12

> **Append-only activity log** for plan 00009. One file per day
> (`00009-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 09:04 · finding · T3.3 — PR 23648 still open; credit dispute on it

Checked again: `OPEN`, not merged, two comments. A maintainer objected that their patch was
taken verbatim with only our reproducer's test added, and only the PR author's name in the
`NEWS` entry; the author apologised and corrected the entry. Nothing for us to do there —
our contribution is the reproducer, credited in the test — but it may slow the merge.

## 09:05 · decision · T3.3 — resolved by defending the constant, not by waiting

See 00007's journal for the same date: `OpcacheDefectRangeTest` makes the build demand the
`FIRST_FIXED` edit on the first fixed PHP. This plan's remaining task was that edit; it no
longer needs a plan to hold it. Closed.

## 09:06 · handoff · — — closed

Filed, verified, fix proposed upstream with our reproducer, policy produced, our own account
corrected to upstream's verdict. Moved to `Completed/`.
